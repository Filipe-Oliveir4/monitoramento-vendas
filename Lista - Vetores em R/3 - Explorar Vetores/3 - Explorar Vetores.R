#Criar vetor numérico
vetor_numerico <- c(10, 20, 30, 40, 50)

#Tamanho do vetor
tamanho_vetor <- length(vetor_numerico)

#Somando constante
vetor_numerico <- vetor_numerico + 2

#Printa vetor
print(vetor_numerico)